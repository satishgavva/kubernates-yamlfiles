apiVersion: v1
kind: Service
metadata:
  name: sujata-service
  labels:
    app: suji
spec:
  ports:
    - nodePort: 32008
      port: 35000
      targetPort: 80
  selector: 
    app: suji  
  type: NodePort   