---
apiversion: v1
kind: pod
metadata:
  name: satish-pod
  labels:
    app: nginx
spec:
  containers:
    - container: satish-cont
      image: nginx
      ports:
        - containerport: 80
          protocol: TCP
             
         

