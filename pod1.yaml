---
apiVersion: v1
kind : pod
metadata: 
  name: satish-pod
  labels:
    app: nginx
spec:
  - containers:
      name: satish-cont
      image: nginx
      ports:
        - containerport: 80
          protocol: TCP

---
apiVersion: v1
kind : service
metadata:
  name: satish-service
  labels:
    app: nginx
spec:
  - ports:
      - NodePort: 32006
        port: 35000
        targetport: 80
    selector:
      app: nginx
    type: NodePort      
           